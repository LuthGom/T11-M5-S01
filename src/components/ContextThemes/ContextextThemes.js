// importamos o hook createContext do react para que possamos criar um componente que recebe valores consideraos globais a toda a aplicação react;
import { createContext } from "react";

export const ContextThemes = createContext()
